.DS_Store
*.log
previews/*.bmp

# local-only: may hold your Telegram token / API keys and runtime logs
worker-config.json
worker-log.jsonl

# local deploy artifact
.netlify-site

# internal agent instructions — never publish
AGENTS.md

# Local Netlify folder
.netlify

# local deps / netlify cli state
node_modules/
.netlify/
package-lock.json
